{
  "name": "mj-weaves-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Studio for MJ Weaves storefront (deployed via `npx sanity deploy`, not part of the static site build)",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sanity": "^3.60.0",
    "styled-components": "^6.1.15"
  },
  "devDependencies": {
    "@sanity/vision": "^3.60.0"
  }
}
